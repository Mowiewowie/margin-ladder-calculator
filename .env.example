# Copy this file to ".env" and paste your free Finnhub API key.
# Get one at https://finnhub.io/ (free tier: 60 quote calls/minute).
# Note: a Vite client-side key is visible in the built bundle — restrict it by
# domain in the Finnhub dashboard if you deploy this publicly.
VITE_FINNHUB_API_KEY=
